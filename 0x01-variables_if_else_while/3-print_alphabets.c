#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/**
 * main - return alphabet 
 *
 * Return: Always zero
 */
int main(void)
	int ch = 'a';

	while (ch <= '2')
{
	putchar(ch);
	ch++;
}
putchar('\n');
return (0);

